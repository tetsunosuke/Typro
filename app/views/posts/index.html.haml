.header
  .left-content
    %h1.main-logo Typ-Pro
  - if user_signed_in?
    .right-content
      = link_to "MyPage",  user_path(current_user),class:"btn"
      = link_to "LogOut", destroy_user_session_path, method: :delete,class:"btn"
  - else
    .right-content
      .right-content__wrapper
        = link_to "ログイン", new_user_session_path,class:"btn"
        = link_to "新規登録", new_user_registration_path, class: "btn"
      .defoult-message
        登録するとスコアが確認できます。
        


.main
  .main-image__messages
    %h2 タイピングでプログラミングを学ぼう！
    = link_to "Start!", new_post_path, class: "start-btn"
  - if user_signed_in?
    .day-score
      %p.title スコア一覧
      - @posts.each do |post|
        .scores__date
          = post.created_at.strftime("%m-%d %H:%M")
          .scores__point
            挑戦者:
            = post.user.name
            = post.score
            得点

   


